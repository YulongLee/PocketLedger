# Statistics

## ADDED Requirements

### Requirement: Period analysis
The app MUST support week, month, year, and custom periods with income/expense switching.



#### Scenario: Basic behavior
- **WHEN** the user uses this capability with valid data
- **THEN** the app completes the requested action and preserves the documented state
### Requirement: Charts and ranking
The app MUST show category distribution, trend, and top category ranking, with a meaningful empty state when no data exists.


#### Scenario: Basic behavior
- **WHEN** the user uses this capability with valid data
- **THEN** the app completes the requested action and preserves the documented state
