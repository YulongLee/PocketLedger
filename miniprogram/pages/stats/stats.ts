Page({data:{expense:'0.00',items:[] as any[],bars:[20,35,24,56,30,70,42,50,28,44]},onShow(){const rs=wx.getStorageSync('records')||[];const ex=rs.filter((r:any)=>r.type==='expense');const total=ex.reduce((s:number,r:any)=>s+r.amount,0);const map:any={};ex.forEach((r:any)=>{map[r.category]??={name:r.category,icon:r.icon,amount:0};map[r.category].amount+=r.amount});this.setData({expense:total.toFixed(2),items:Object.values(map).map((x:any)=>({...x,percent:total?Math.round(x.amount/total*100):0}))})}})
