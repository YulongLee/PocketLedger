Page({data:{privateAmount:false,aiEnabled:true},onLoad(){const s=wx.getStorageSync('settings')||{};this.setData({privateAmount:!!s.privateAmount,aiEnabled:s.aiEnabled!==false})},togglePrivate(e:any){const v=!!e.detail.value;const s=wx.getStorageSync('settings')||{};s.privateAmount=v;wx.setStorageSync('settings',s);this.setData({privateAmount:v})},toggleAI(e:any){const v=!!e.detail.value;const s=wx.getStorageSync('settings')||{};s.aiEnabled=v;wx.setStorageSync('settings',s);this.setData({aiEnabled:v})},clearCache(){wx.showModal({title:'清除本地缓存？',content:'不会删除云端数据',success:r=>{if(r.confirm){wx.removeStorageSync('pocketledger_transactions');wx.showToast({title:'已清除'})}}})}})
