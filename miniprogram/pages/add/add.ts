Page({data:{type:'expense',amount:'',note:'',index:0,categories:[{name:'餐饮',icon:'🍜'},{name:'交通',icon:'🚗'},{name:'购物',icon:'🛍️'},{name:'工资',icon:'💰'}]},setType(e:any){this.setData({type:e.currentTarget.dataset.type})},amountInput(e:any){this.setData({amount:e.detail.value})},noteInput(e:any){this.setData({note:e.detail.value})},cat(e:any){this.setData({index:Number(e.detail.value)})},save(){const n=Number(this.data.amount);if(!n){wx.showToast({title:'请输入金额',icon:'none'});return;}const c=this.data.categories[this.data.index];const now=new Date();const r={id:String(Date.now()),amount:n,type:this.data.type,category:c.name,icon:c.icon,date:`${now.getFullYear()}-${now.getMonth()+1}-${now.getDate()}`,note:this.data.note};const rs=wx.getStorageSync('records')||[];rs.push(r);wx.setStorageSync('records',rs);wx.navigateBack()}})
