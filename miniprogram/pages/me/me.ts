import { api } from '../../services/api'
Page({async clear(){wx.showModal({title:'删除云端全部账单？',content:'此操作不可恢复',success:async r=>{if(r.confirm){try{await api.deleteData();wx.removeStorageSync('access_token');wx.showToast({title:'数据已删除'})}catch(e){wx.showToast({title:'请先登录',icon:'none'})}}}})},async export(){try{const data=await api.exportData();wx.setClipboardData({data:JSON.stringify(data),success:()=>wx.showToast({title:'已复制云端数据'})})}catch(e){wx.showToast({title:'导出失败，请重试',icon:'none'})}},accounts(){wx.navigateTo({url:'/pages/accounts/accounts'})}})
